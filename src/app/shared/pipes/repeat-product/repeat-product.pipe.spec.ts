import { RepeatProductPipe } from './repeat-product.pipe';

describe('RepeatProductPipe', () => {
  it('create an instance', () => {
    const pipe = new RepeatProductPipe();
    expect(pipe).toBeTruthy();
  });
});
